public class Engine3D {
    private Environment environment = new Environment();
    private Renderer renderer = new Renderer();
    private Camera camera = new Camera();

    public Engine3D() {

    }

    private void update() {
        // environment.update();
        // camera.toScreen();
        // renderer.render(screenObjects);
    }

}
